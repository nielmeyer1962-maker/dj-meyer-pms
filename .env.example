# Generate a strong, unique value before deploying — e.g.
#   python -c "import secrets; print(secrets.token_hex(32))"
# The app REFUSES to start while SECRET_KEY is this placeholder (unless TESTING or FLASK_DEBUG).
SECRET_KEY=change-me-in-production
DATABASE_URL=postgresql://postgres:password@localhost/djmeyer_pms
TEST_DATABASE_URL=postgresql://djmeyer_app:REPLACE_ME@localhost:5433/djmeyer_pms_test
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-gmail@gmail.com
MAIL_PASSWORD=your-app-password
