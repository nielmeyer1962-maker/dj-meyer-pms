# Python
__pycache__/
*.py[cod]
*.so
.venv/
venv/
*.egg-info/
dist/
build/

# Environment
.env
.env.local

# Testing
.pytest_cache/
htmlcov/
.coverage

# Ruff
.ruff_cache/

# Database
*.sqlite3

# Local dev scripts
seed.py

# Import run artefacts — timestamped data-quality reports written by `flask
# import-clients` (may echo client names; POPIA — keep out of git).
/var/

# Client data — never commit (POPIA). Raw client spreadsheets/exports stay on Y:\.
*.xlsx
*.xls
*.csv
*.pdf
Excell files to be imported into PMS/
# Local SQL dumps (schema.sql + seed.sql with real client rows). Schema lives in
# migrations/; force-add database/schema.sql if you ever want it tracked.
/database/
# Loose client-data drops in the repo root
/Damian s Whatsup.txt

# Editor
.vscode/
.idea/
*.swp
# Ignore local/personal Claude Code config, but track the shared project settings
# (settings.json carries the team-wide ruff-format hook).
.claude/*
!.claude/settings.json
Tools/
drafts/